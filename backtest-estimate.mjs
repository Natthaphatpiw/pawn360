/**
 * Retired on 2026-08-01.
 *
 * The former script bypassed production safeguards and used obsolete models
 * and search tooling. See backtest-estimate.mts for the migration rationale.
 */

throw new Error(
  'LEGACY_BACKTEST_RETIRED: use the current queued estimate pipeline and cost:llm-smoke',
);
